# MealPlannerNew
Rebuilt the meal planner app with all the latest packages.

To create a new *.apk file for distributing to android testers you run the following code in a terminal.
eas build --platform android --profile preview

To run the app on an emulator open "Android Emulator" then type the following into a terminal:
npm start